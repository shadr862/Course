#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define pb push_back
#define mp make_pair
const ll INF=1e18;
const double pii = acos(-1);
const int mod = 1000000007;
const int N = (int) 1e5;

ll lcm(ll a,ll b)
{
    return (a/__gcd(a,b))*b;
}

int fo[N], fe[N];
int sod[N+1];
int nod[N+1];
inline void generatesod()
{
    for(int i=1;i<=N;i++)
    {
        for(int j=i;j<=N;j+=i)
        {
            sod[j]+=i;
        }
    }
}
inline void generatnod()
{
    for(int i=1;i<=N;i++)
    {
        for(int j=i;j<=N;j+=i)
        {
            nod[j]+=1;
        }
    }
}

inline void solve()
{

    ll n,x;
    cin>>n>>x;
    ll arr[n];

    for(ll i=0;i<n;i++)cin>>arr[i];
    sort(arr,arr+n);
    ll l,r;

     for(ll i=0;i<n;i++)
     {
        l=i+1;
        r=n-1;
        while(l<r)
        {
            ll sum=arr[i]+arr[l]+arr[r];
            if(sum==x)
            {
                cout<<arr[i]<<" "<<arr[l]<<" "<<arr[r]<<endl;
            }
            if(sum>x)r--;
            else{
                l++;
            }
        }
     }



}
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {

        solve();

    }

	return 0;
}