#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define pb push_back
#define mp make_pair
const ll INF=1e18;
const double pii = acos(-1);
const int mod = 1000000007;
const int N = (int) 1e5;

ll lcm(ll a,ll b)
{
    return (a/__gcd(a,b))*b;
}

int fo[N], fe[N];
int sod[N+1];
int nod[N+1];
void generatesod()
{
    for(int i=1;i<=N;i++)
    {
        for(int j=i;j<=N;j+=i)
        {
            sod[j]+=i;
        }
    }
}
void generatnod()
{
    for(int i=1;i<=N;i++)
    {
        for(int j=i;j<=N;j+=i)
        {
            nod[j]+=1;
        }
    }
}

void solve()
{
 generatesod();
 generatnod();
 ll n;
 cin>>n;
 cout<<sod[n]<<endl;
cout<<nod[n]<<endl;





}
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        solve();

    }

	return 0;
}
