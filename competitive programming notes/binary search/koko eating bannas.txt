#include <bits/stdc++.h>
using namespace std;
#define ln "\n"
#define ll long long
#define pb push_back
#define mp make_pair
const ll INF=1e18;
const double pii = acos(-1);
const ll mod = 1e9+7;
const ll N = 2e5+5;
ll cnt[N+1][6];

ll lcm(ll a,ll b)
{
    return (a/__gcd(a,b))*b;
}
bool ok(ll mid , ll arr[] ,ll n, ll h){


        ll total=0;

        for(int i=0;i<n;i++){
            total+=ceil(double(arr[i]/(double)mid));
        }
        if(total<=h)
        {
            return true;
        }

        return false;
    }




inline void solve1()
{
    ll n,h;
    cin>>n>>h;
    ll arr[n];
    for(ll i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    ll l=1;
    ll r=1e18;
        while(l<r){
           ll mid=(l+r)/2;
            if(ok(mid,arr,n,h))
                r=mid;
            else
                l=mid+1;
        }
        cout<<l<<endl;



}

int main()
{
    ll t;
    cin>>t;

    while(t--)
    {

       solve1();

    }

}
