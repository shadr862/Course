https://codeforces.com/problemset/problem/1547/D
#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define pb push_back
#define mp make_pair
const ll INF=1e18;
const double pii = acos(-1);
const int mod = 1000000007;
const int N = (int) 1e5;

ll lcm(ll a,ll b)
{
    return (a/__gcd(a,b))*b;
}

int fo[N], fe[N];
11=1011
13=1101
if(a&b)we get 9=1001
9^11=0010=2
2^13=0010^1101=1111
void solve()
{
        int n;
        cin>>n;
        vector<int> x(n);
        for(auto& i:x) cin>>i;
        vector<int> res;
        res.push_back(0);
        for(int i=1; i<n; i++){
            int tmp = ((x[i]) & (x[i-1]));
            tmp^=x[i-1];
            res.push_back(tmp);
            x[i] ^= tmp;
        }
        for(auto i:res) cout<<i<<" ";
        cout<<"\n";
}
void solve()
{
                int n;
		cin >> n;
 
		vector<int> a(n);
		for (int i = 0; i < n; i++)
			cin >> a[i];
 
		vector<int> ans(n, 0);
 
		for (int i = 0; i < 30; i++)
		{
			int st = 0;
			for (int j = 0; j < n; j++)
			{
				if ((a[j] & (1 << i)))
				{
					st = 1;
					continue;
				}
				if (st)
					ans[j] |= (1 << i);
			}
		}
 
		for (auto x : ans)
		{
			cout << x << " ";
		}
		cout << endl;
	}
}
void solve()
{
      ll n,b,j;
      cin>>n;
      ll a[n];
      for(ll i=0;i<n;i++)
      {
          cin>>a[i];
      }
       b=a[0];
      vector<ll> ans;
      ans.pb(0);
      for(ll i=1;i<n;i++)
      {
          ll x=b;
          j=1;
          ll sum=0;
          ll xx=a[i];
          while(x)
          {
              ll y=(x&1);
              ll z=(a[i]&1);
              if(y==1&&z==0)
              {
                  sum+=j;
              }
              j*=2;
              x>>=1;
              a[i]>>=1;

          }
          ans.pb(sum);
          b=xx^sum;

      }
      for(auto x:ans)
      {
          cout<<x<<" ";
      }
      cout<<endl;

}
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        solve();

    }

	return 0;
}
