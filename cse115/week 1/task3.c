#include<stdio.h>
int main()
{
    int a;
    float b;
    double c;
    char d;
    printf("size is=%d bytes\n",sizeof(a));
     printf("size is=%d bytes\n",sizeof(b));
      printf("size is=%d bytes\n",sizeof(c));
       printf("size is=%d bytes\n",sizeof(d));
       return 0;
}
